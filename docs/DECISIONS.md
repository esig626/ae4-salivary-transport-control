# Decision log

| Date | Decision | Reason | Alternatives considered | Consequence |
| --- | --- | --- | --- | --- |
| 2026-08-27 | Treat the historical project as immutable reference material | Preserve provenance and separate collaborative historical work from the new project | Continue directly from old manuscript and code | All new results require independent derivation from published sources |
| 2026-08-27 | Audit dimensionless control before accepting the old sensitivity explanation | Raw derivatives across differently scaled parameters are not directly comparable | Reuse the historical derivative magnitude argument | Phase 03 is a mandatory gate before mechanism claims |
| 2026-08-27 | Use `literature/core/2018_AE4_model.pdf` as the Phase 00 primary model source | It is the published, citable baseline requested by the research plan | Use the duplicate archive copy or historical manuscript/code | The new source map has no dependency on unpublished material |
| 2026-08-27 | Leave internally inconsistent published equations unresolved in Phase 00 | Selecting a sign, unit conversion, or closure without evidence would turn an ambiguity into an undocumented assumption | Infer intent from physiology or inspect historical MATLAB | Phase 01 is blocked on the critical items listed in `analysis/00_inventory/open_questions.md` |
| 2026-08-27 | Use `d omega_i/dt = q_b - q_a` as the conservation-consistent dynamic volume sign, while proving steady-state results are sign-invariant | The published definitions make `q_a` cell-to-lumen and `q_b` bath-to-cell; both printed signs impose `q_a=q_b` at equilibrium | Silently select Eq. (9), or abandon every steady identity because of the typo | The dynamic correction is documented, and none of the reported steady-state algebra depends on it |
| 2026-08-27 | Apply the upstream Palk corrigendum to NKCC1 rate units while leaving unrelated NaK, Ae2, and density conventions unresolved | The corrigendum explicitly gives M-based coefficients and correct time dimensions; it does not correct the other 2018 parameter families | Treat every large exponent as a typo, or ignore the published correction | The corrected NKCC1 turnover is regression-tested, but it does not unblock the full model |
| 2026-08-27 | Do not construct a numerical full-model baseline by choosing undocumented unit, sign, or calibration conventions | The printed baseline fails the water, apical-current, and acid/base closures; reusing the printed AE4 fourth-order constants with M-valued states creates a `10^12` error, while the required whole-cell scale is absent | Select the most physiologically plausible convention, fit missing conversions, or use unpublished historical code | Physiological panel ranks, observation surfaces, perturbation designs, and mechanism error rates remain uncertified |
| 2026-08-27 | Limit Phase 10 evidence to exact exchanger-flux geometry and direct printed-equation diagnostics; retain the affine diagnostic only as a labeled software fixture | These quantities are reproducible without pretending that the full published equilibrium has been reconstructed | Publish a surrogate as though it were the AE4 model, or discard useful derivative test infrastructure | Every surrogate panel rank and figure is explicitly non-physiological and excluded from the paper decision |
| 2026-08-27 | Classify the quick-paper project as `STOP` and do not draft a manuscript | The only surviving model-specific statement is a conditional two-flux decomposition; its generic rank machinery is established, and the source blockers prevent the requested physiological minimal-panel result | `QUICK PAPER` based on routine local theory, or `PUSH HARDER` based on uncomputed perturbation geometry | Resume only with a corrected executable specification or author clarification sufficient to reproduce the baseline |
| 2026-08-27 | Grade the archived MATLAB snapshot `UNLIKELY` to be the exact implementation that generated the 2018 article, while recording strong direct lineage | All 45 `Par.mat` fields and several geometry-scaled published activities trace to the Original calibration, but the archived equations use Na-only AE4, different channel/NHE1/acid-base/calcium conventions, stale dependencies, and do not reproduce the approximately 24% AE4-knockout flow loss | Call it the published implementation from shared names/parameters; reject all relationship because the equations differ; grade `PLAUSIBLE BUT UNPROVEN` | Historical files may explain ancestry and limited conventions, but cannot serve as the validated 2018 executable |
| 2026-08-27 | Accept only the conservation volume sign, corrected NKCC mM conversion, signed `I/F` bookkeeping, recovered geometry mappings, historical solver tolerances, and likely AE2 `K_B` minus sign as resolved or strongly corroborated conventions | These items have algebraic, conservation, or independent public-source support; the other historical changes are fitted or scientifically different | Import every historical calibration into the published model; discard every convention because exact code identity failed | Each accepted convention is labelled as public correction or historical implementation evidence; water, current, acid/base, AE4, flow, and time scales remain separately qualified |
| 2026-08-27 | Keep the clean-reconstruction and physiological-identifiability gates closed and retain `STOP` | Identity is below the required `VERY LIKELY` threshold; the historical baseline is constructed rather than an independent published-table reproduction; no available trajectory reproduces the central AE4 phenotype; no trustworthy parameter-to-observation map exists | Build `src/reconstructed_2018/` using best-guess conventions; run identifiability on the historical reduction; promote unpublished continuation results | No clean model, new physiological derivatives/panels, or manuscript was created; restart requires publication-generating source or a uniquely specified correction plus baseline/phenotype regression |
