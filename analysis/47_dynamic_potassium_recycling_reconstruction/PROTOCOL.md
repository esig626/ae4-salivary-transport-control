# Task 47 protocol

Base: `acd554eb430ae25968ef56e3a9acdd816ca4e688`.
Branch: `analysis/task-47-dynamic-potassium-recycling-reconstruction`.

AGENTS.md was read first. The explicit Task 47 assignment supersedes its manuscript assignment. The Task 46 handoff, protocol, evidence split and status were read, followed by the pinned Task 43 provenance and Task 44 mathematical outputs. H00 to H04 are completed structural work. There will be no CBM solve, new mechanism family, optimisation, parameter sweep or genotype specific resting solve.

The source tree is materialised through the connected GitHub integration at the exact base. The supplied Task 47 branch initially points to that base. Publication uses new trees based on the preceding full remote tree, one parent per commit and a nonforced update of this branch only. Frozen files and main remain unchanged. Task 43 and Task 44 are read at their handoff pins, respectively `547f113d9123ab1976774639a69483faf40cef34` and `e5fa9840bf96be3147c6118daee4942468c78f8b`; they are not merged.

## Evidence and decision rule

The null magnitude is already known from the question and historical repository reports. This is a freeze before comparison, not a claim that the analyst has never seen the phenotype. No value, threshold, candidate or stopping decision will be selected to approach it. The 5% condition is deferred until after the remote prediction freeze.

First reconstruct the exact active equations and provenance. A model limitation or uncertain parameter alone does not identify a numerical correction. Change production physiology only if an equation or independently supported parameter correction is identifiable. If the reserve is sufficient in the inherited model but its biological value is unidentified, report those two statements separately. Do not call inherited values measured or declare general biological falsification.

## Bounded dynamic work

Use the unchanged Task 40 WT parameter hashes and saved WT rest. First run WT and null for the inherited 600 s stimulus, each once, with the inherited Radau tolerances and onset. Check every accepted endpoint and every integer second. Use the inherited trapezoidal observation grid for cumulative secretion and 60 to 600 s integrals. No stationary continuation or old sensitivity work is repeated.

Record channel currents, reversal potentials and driving voltages; K and Na amount storage and concentration dilution; pump cycles, Na clearance and utilisation of both nominal capacity and the instantaneous external K conditioned ceiling; NKCC flux, concentration product and both the selected law and ideal bath affinities; chloride export, water output and all inherited physiological gates.

The ratio of an Ohmic current to `g_active*(V-E_K)` is an equation check, equal to one, not a reserve estimate. The ratio to `g_max*(V-E_K)` is the calcium gate, not recruitable reserve at fixed calcium. No current bound will be inferred from the numerical voltage bracket. Derive the dynamic storage correction to the CBM identity and verify it without putting it into the RHS.

Use exact local derivatives for the NKCC concentration product, pump Na saturation and the two voltage equations. At 60, 300 and 600 s in WT and null, verify only the selected K conductance and pump capacity derivatives with symmetric relative log steps `1e-4` and `5e-5`. These are instantaneous evaluations at saved states, with no altered trajectory, search or tuning.

Publish the audit; publish baseline dynamics; record the correction decision; publish an immutable prediction manifest with source, parameter, initial state and output hashes. Verify that remote commit before evaluating the 5% case or writing any comparison with the known magnitude. No parameter changes follow the freeze. Finally publish validation, compact figures, executable checks and the article report. Stop there.
