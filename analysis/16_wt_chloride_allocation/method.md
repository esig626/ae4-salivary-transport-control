# Task 16 method and phenotype firewall

The scientific baseline is completed Task 14B commit `4d4403f279fc36aec940c9e4759486d02f07e3f6`, tree `386700aad2f807ba9d1de93335b6bbdd92e5cef6`. Task 16 intake `fa59ef7670218125feccc5db72134995afd8fece` changes only instructions and prompts relative to that scientific baseline. Its AGENTS.md explicitly supersedes the old Task 15 remote-push prerequisite. The accepted Task 15 sign/source audit is diagnostic context. No Task 15 audit was rerun or imported as a scientific model change.

The existing 187-path input inventory was used only to identify frozen bytes, then independently checked against their SHA-256 hashes, byte sizes and Git blob identities at Task 14B. All 187 are unchanged. The current checkout uses an exact verified remote Git tree; unrelated historical files absent from the local object cache remain unmaterialized and unchanged in that tree. No archive content is edited. The current task creates separate orchestration, diagnostics, tests and result artifacts; all inherited scientific source files remain unchanged.

The allocation contract was committed locally as `9b6d6bb` before the modified solves started, and its identical tree was published as `b1f2697bb20cfe7456bb4ea1d45060bee6669638`. Contract SHA-256 is `19b95a1e76b0a504784f1f65e71168801c3751414d474e08076aec119bb6929b`. Its only scientific conditions are inherited baseline, share 0.10 and share 0.30, for all ten roots and both routing families. These shares are sensitivity coordinates, not measured fractions. There is no share refinement or threshold search.

For each original resting state, A0 is the complete AE4 intracellular chloride source, N0 is twice the NKCC1 cycle flux, and E0 is the signed AE2 source. All ten A0 and N0 values are positive; all ten E0 values are negative. The positive-loading denominator is exactly A0+N0. At target f, lambda=f(A0+N0)/A0 and mu=(1-f)(A0+N0)/N0. The reference loading sum is preserved; the realized equilibrium share need not equal f.

New parameter objects replace only `AE4Parameters.carrier_amount_fmol` and `FullModelParameters.homeostasis.nkcc1_capacity_fmol_s`. AE4 Na, K, Cl, HCO3, TIC and alkalinity sources scale together, as does the entire NKCC1 Na:K:2Cl cycle. The graph, branch attempts/routing, affinities, forward/reverse ratios, reversal surfaces and regulation remain fixed. The complete whole-cell RHS difference is tested against these two stoichiometric source changes. No chloride-only source is scaled and no genotype compensation is introduced.

All other parameters, bath, geometry, OTHER osmoles, acid-base chemistry, water/outflow, calcium values 0.10/0.25/0.50 uM, CCh+IPR, 600 s duration, `R1_G125_P21_PROSE_TREFERENCE`, and solver tolerances are inherited exactly. In particular, the NKCC1 stimulation normalization remains 0.10 uM with a 1.75 multiplier even for the higher input calcium values.

The continuation path is lambda(t)=lambda_target**t and mu(t)=mu_target**t. It starts with internal t increments of 0.25, halves an increment after a failed numerical solve or disconnected root, and stops at the predeclared minimum increment 0.25/256. These are internal numerical steps toward the two specified endpoints, never scientific candidate allocations. They trigger no dynamics or genotype simulations. All 467 internal step attempts, all three local starts per attempt, alternate numerical roots and distance decisions are preserved. No crossing share is estimated or reported.

At each step the unchanged `genotype_evaluation._attempt_and_root` kernel is used with genotype WT, expression 1.0, ten state coordinates, zero fitted parameters, three starts offset by 0 and +/-0.001 coordinate spans, and max_nfev 3000. Root selection uses distance from the previous selected root divided by coordinate spans, with residual as tie-breaker; the maximum connected distance is 0.25. Internal points need the inherited numerical gates. Endpoint candidates additionally need the inherited physiological gates. The original WT roots are reused, with direct residual/rank verification and no resting re-solve.

The inherited resting gates are Cl 47.10–53.10 mM, pH 6.77–7.05, Na 10–35 mM, K 100–160 mM, cell volume 1.17–1.43 pL, apical voltage -80 to -30 mV and basolateral voltage -90 to -35 mV, plus positive luminal quantities. Numerical gates retain the complete root residual, rank, charge, current, domain, carbon/alkalinity and water consistency requirements. The search domain is broader than physiological acceptance: notably Na 4–60 mM and K 60–210 mM. Neither domain nor tolerance is expanded in Task 16.

For every rest-admissible modified endpoint, the contract requires production Radau at all three calcium values, on the inherited 602-point grid. Dynamic admission requires complete integration, positive core, finite nonnegative flow, inherited conservation/current limits, charge <=1e-9 fmol, and the inherited WT Q600/Q60 sustainment interval [0.8,1.2]. No new stimulated-ion band is invented. Absolute one-SMG mapping and the shared minute observation scale remain nonblocking, as does a change in per-cell WT total. AE4-null time-course shape is not used.

The minimal new WT BDF rule is the lexicographically first rest-admissible root per modified share/routing at calcium 0.50 uM, expanding to that share/routing if it disagrees. Because no modified endpoint passes rest, no new BDF check is triggered. All 30 inherited WT trajectories and six inherited representative WT solver confirmations are reused after hash checks. No new ODE integration is performed during the WT stage.

The complete 90-row root/share/calcium decision table is frozen before genotype access. Hashes cover every parameter/root/WT trajectory payload, decision, WT runner, diagnostic module and WT test. The frozen checkpoint must be committed and pushed before the separate post-freeze evaluator can read genotype results. Every frozen combination remains in final accounting. The post-freeze module has no authority to construct or revise WT candidates.
