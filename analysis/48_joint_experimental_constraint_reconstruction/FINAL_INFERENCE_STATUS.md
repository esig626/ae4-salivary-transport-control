# Final reconstruction status

The authorised negative stopping condition is reached. There is no accepted corrected architecture and no completed joint numerical fit. The final data ledger, unchanged shared vector, genotype resting states and supported predictions remain available for scientific review.

48E does not relabel baseline diagnostics as a fitted reconstruction. `output/final_residuals.csv` and `.json` are exact copies of the 48C residual records. `output/parameter_vector.json` explicitly identifies the vector as inherited and unfitted. The 48D correction gate, published before this final decision, accepts no new mechanism. No model run or parameter change follows the final gland comparison.

The identifiability record contains the exact zero Jacobian of the conflicting knockout stimulus contrast across all 33 provenance eligible candidate directions. It is not a full numerical observation Jacobian. The full map is undefined for blocked assays. Exact scale redundancies are retained and no parameter confidence intervals or optimum covariance are fabricated.

The 2015 SEs describe estimated population means. Residuals for directly mapped values use those SEs. The saliva deficit is scored in its reported percent units. Bicarbonate uses genotype ratios with a Gaussian profile contrast that retains uncertainty in both group means; raw amounts are not also scored. Fluorescence measurements without calibration and windows remain unavailable except for analytically invariant zero effects. Qualitative nonsignificance receives no percentage tolerance. Cross observable covariance and animal clustering remain unreported, so no calibrated global likelihood p value is supplied.

Secondary parotid knockout studies are documented but not numerically cross validated: the prerequisite primary fit did not succeed. An exact deleted source is not validation of a gland secretion magnitude, and the chronic NKCC expression adaptation reported with NHE1 loss is absent from this shared deletion model.

The scientific inference is restricted but decisive within its assumptions: the inherited beta pathway confined to AE4 cannot produce the observed stimulus dependence in cells lacking AE4. Resting chloride, resting pH and sustained secretion also remain wrong at the unchanged parameter vector. The available evidence does not select a single minimal equation repair. Further fitted flexibility would exceed this task.
