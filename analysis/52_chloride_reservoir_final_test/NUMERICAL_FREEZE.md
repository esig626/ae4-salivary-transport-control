# Task52 immutable numerical freeze —52C

The controlling artifact is `output/parameter_and_case_freeze.json`.
Its SHA256 is `9cebb8fcf85557643f6c7838ff57f9fb9603f5a7479cbe6fb63ba8c40b417ba2`.
The50 immutable artifacts include all byte-exact historical dependencies,
measured onset projections, tested model/projector/diagnostics, the production
runner, binding scientific inputs and provenance. The mandatory ledger prefix
is separately protected so new results may be appended without rewriting history.

No new scientific evaluation occurred. The isolated runtime import audit passes;
all four evaluation counters remain zero. The inherited tolerances, three shared
conductances, three supply multipliers and eight-case matrix are unchanged.

Execution is gated by the independently verified previous checkpoint, exact
branch/HEAD,52C receipt, immutable hashes and one-thread runtime limits. Each case
directory and stage receipt may be created once only. A numerical-status failure
allows the single inheritedBDF fallback; physical failure and exceptions do not.

NPZ records are pickle-free numeric/string arrays. They preserve all paired states,
full diagnostics at accepted/integer samples, delta(t), cumulative independent
flux quadrature, and raw accepted states. An already accepted endpoint beyond the
first failed dense sample is retained but marked unvalidated. JSON summaries
include complete or explicitly partial budgets, windows, extrema and failures.
Positive chloride driving force means `E_Cl-V_a`; `J` is total outward apical Cl.
