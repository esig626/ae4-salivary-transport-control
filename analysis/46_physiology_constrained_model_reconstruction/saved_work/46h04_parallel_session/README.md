# Saved Task 46H04 parallel session

Saved at the user's explicit instruction to publish existing work and stop. No further simulations or scientific calculations were performed.

This directory preserves the completed paired calculations, full witnesses, utility tests, independent review, code and checkpoint material from the session whose publication encountered concurrent remote changes. Original relative paths and exact bytes are retained. The existing remote H04 implementation and immutable checkpoints are not overwritten.

These are archived source files, not an additional active model architecture. The archived CURRENT_STATUS.md describes the saved session, not current branch status. H05 through H07 were not completed by this session and no final CBM decision report is claimed.

The code expects its original Task 46 directory layout and the frozen vendor/carbonscope_flux_core dependency already in the repository. For later reproduction, use an isolated copy of that layout with the archived cbm files; do not mix these utilities with another implementation. SAVED_WORK_MANIFEST.json lists exact file hashes. No reproduction was run when saving.
